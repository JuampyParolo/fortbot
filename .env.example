# ══════════════════════════════════════════
# FORTBOT v0.4 — Configuration
# ══════════════════════════════════════════
# Prerequisite: run `claude login` first

# YOUR personal WhatsApp number (with country code, no +)
OWNER_NUMBER=5491123456789

# Optional: trusted contacts (comma-separated)
KNOWN_CONTACTS=

# ── LLM Models ───────────────────────────
# Options: sonnet (best), opus (strongest), haiku (fastest)
PLANNER_MODEL=sonnet
QUARANTINE_MODEL=haiku

# Local LLM for quarantine (max privacy — data never leaves network)
USE_LOCAL_QUARANTINE=false
LOCAL_LLM_ENDPOINT=http://localhost:11434

# ── Security ─────────────────────────────
# Kill switch (send via WhatsApp to stop everything)
KILL_SWITCH=/fortbot-stop

# ── Storage ──────────────────────────────
DB_PATH=./fortbot.db

# ── Human Emulation (anti-detection) ─────
# Active hours (bot "sleeps" outside these)
HUMAN_WAKE_HOUR=8
HUMAN_SLEEP_HOUR=23

# Rate limits (lower = safer from detection)
HUMAN_MAX_MSG_MIN=4
HUMAN_MAX_MSG_HOUR=30

# Gateway: whatsapp (default) or cli (terminal testing)
FORTBOT_GATEWAY=whatsapp
